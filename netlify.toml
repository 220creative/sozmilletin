# Netlify yayın yapılandırması
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Tek sayfa uygulaması: bilinmeyen yollar index.html'e yönlensin
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
